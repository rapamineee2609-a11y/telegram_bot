{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt",
    "nixpacks": {
      "aptPackages": ["ffmpeg"]
    }
  },
  "deploy": {
    "startCommand": "python bot.py",
    "healthcheckPath": "/"
  }
}
