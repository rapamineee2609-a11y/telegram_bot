{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install python3 python3-pip ffmpeg -y && python3 -m pip install python-telegram-bot==20.8 yt-dlp requests Pillow python-dotenv loguru aiohttp",
    "nixpacks": {
      "aptPackages": ["ffmpeg"]
    }
  },
  "deploy": {
    "startCommand": "python3 bot.py"
  }
}
